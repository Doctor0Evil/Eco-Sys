[package]
name = "mpc_runner"
version = "0.1.0"
edition = "2021"

[dependencies]
contracts_core = { path = "../contracts_core" }
mpc_kernel = { path = "../mpc_kernel" }
mpc_constraints = { path = "../mpc_constraints" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
